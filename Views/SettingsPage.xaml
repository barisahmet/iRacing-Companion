<UserControl x:Class="IRacingSmartPlug.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:IRacingSmartPlug.ViewModels"
             d:DataContext="{d:DesignInstance vm:SettingsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
  <UserControl.Resources>
    <Style x:Key="Divider" TargetType="Border">
      <Setter Property="Height" Value="1" />
      <Setter Property="Background" Value="{DynamicResource CardStrokeColorDefaultBrush}" />
      <Setter Property="Opacity" Value="0.5" />
      <Setter Property="Margin" Value="0,3" />
    </Style>
  </UserControl.Resources>

  <Grid Margin="20,6,20,12">
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="0,0,6,0">
      <StackPanel>
        <TextBlock Text="Settings" Style="{StaticResource PageTitle}" Margin="2,8,0,16" />

        <!-- Home Assistant -->
        <ui:Card Padding="20,10" Margin="0,0,0,12">
          <StackPanel>
            <TextBlock Text="Home Assistant" Style="{StaticResource SectionHeader}" Margin="0,8,0,2" />
            <TextBlock Text="Connect to the smart plug entity" Style="{StaticResource CardCaption}"
                       Margin="0,0,0,10" />

            <Grid Margin="0,8">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
              <TextBlock Text="Base URL" Style="{StaticResource FieldLabel}" />
              <ui:TextBox Grid.Column="1" Width="340"
                          Text="{Binding BaseUrl, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
            <Border Style="{StaticResource Divider}" />
            <Grid Margin="0,8">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
              <TextBlock Text="Long-lived token" Style="{StaticResource FieldLabel}" />
              <ui:TextBox Grid.Column="1" Width="340" FontFamily="Consolas"
                          Text="{Binding Token, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
            <Border Style="{StaticResource Divider}" />
            <Grid Margin="0,8">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
              <TextBlock Text="Entity ID" Style="{StaticResource FieldLabel}" />
              <ui:TextBox Grid.Column="1" Width="340"
                          Text="{Binding EntityId, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
          </StackPanel>
        </ui:Card>

        <!-- Behavior -->
        <ui:Card Padding="20,10" Margin="0,0,0,12">
          <StackPanel>
            <TextBlock Text="Behavior" Style="{StaticResource SectionHeader}" Margin="0,8,0,2" />
            <TextBlock Text="How and when the plug switches" Style="{StaticResource CardCaption}"
                       Margin="0,0,0,10" />

            <Grid Margin="0,8">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
              <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Power plug on" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock Text="Which session powers the rig" Style="{StaticResource CardCaption}" Margin="0,2,0,0" />
              </StackPanel>
              <ComboBox Grid.Column="1" Width="220" VerticalAlignment="Center"
                        ItemsSource="{Binding PlugTriggers}" DisplayMemberPath="Display"
                        SelectedValuePath="Value" SelectedValue="{Binding PlugTrigger, Mode=TwoWay}" />
            </Grid>
            <Border Style="{StaticResource Divider}" />
            <Grid Margin="0,8">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
              <TextBlock Text="iRacing UI process" Style="{StaticResource FieldLabel}" />
              <ui:TextBox Grid.Column="1" Width="220"
                          Text="{Binding UiProcessName, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
            <Border Style="{StaticResource Divider}" />
            <Grid Margin="0,8">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
              <TextBlock Text="Simulator process" Style="{StaticResource FieldLabel}" />
              <ui:TextBox Grid.Column="1" Width="220"
                          Text="{Binding SimProcessName, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
            <Border Style="{StaticResource Divider}" />
            <Grid Margin="0,8">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
              <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Turn off delay" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock Text="Seconds after iRacing closes" Style="{StaticResource CardCaption}" Margin="0,2,0,0" />
              </StackPanel>
              <ui:TextBox Grid.Column="1" Width="110" VerticalAlignment="Center"
                          Text="{Binding OffDelaySeconds, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
            <Border Style="{StaticResource Divider}" />
            <Grid Margin="0,8">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
              <TextBlock Text="Poll interval (s)" Style="{StaticResource FieldLabel}" />
              <ui:TextBox Grid.Column="1" Width="110"
                          Text="{Binding PollIntervalSeconds, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
            <Border Style="{StaticResource Divider}" />
            <Grid Margin="0,8">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
              <TextBlock Text="Request timeout (s)" Style="{StaticResource FieldLabel}" />
              <ui:TextBox Grid.Column="1" Width="110"
                          Text="{Binding RequestTimeoutSeconds, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
          </StackPanel>
        </ui:Card>

        <!-- Startup -->
        <ui:Card Padding="20,16">
          <Grid>
            <StackPanel VerticalAlignment="Center">
              <TextBlock Text="Start with Windows" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                         FontWeight="SemiBold" />
              <TextBlock Text="Launch minimized to the tray at login" Style="{StaticResource CardCaption}" />
            </StackPanel>
            <ui:ToggleSwitch HorizontalAlignment="Right" VerticalAlignment="Center"
                             IsChecked="{Binding StartWithWindows, Mode=TwoWay}" />
          </Grid>
        </ui:Card>
      </StackPanel>
    </ScrollViewer>

    <!-- Actions -->
    <Border Grid.Row="1" Margin="0,10,0,0">
      <StackPanel Orientation="Horizontal">
        <ui:Button Content="Save" Appearance="Primary" MinWidth="90" Command="{Binding SaveCommand}" Margin="0,0,8,0" />
        <ui:Button Content="Reload" Appearance="Secondary" Command="{Binding ReloadCommand}" Margin="0,0,8,0" />
        <ui:Button Content="Test connection" Appearance="Secondary" Command="{Binding TestConnectionCommand}" />
        <TextBlock Text="{Binding Note}" VerticalAlignment="Center" Margin="16,0,0,0"
                   Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
